server:
  port: 0

spring:
  jpa:
    hibernate.ddl-auto: none
    properties.hibernate.format_sql: true
  sql.init.mode: always
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/product-service
    username: db-user
    password: 12345

logging:
  level:
    ru.yandex.practicum.product: DEBUG